use rust_full_text_engine::index::{init};

fn main() {
    // Initialize indexes at the start of the program
    init();

    // Rest of the program logic
    println!("Indexes initialized. Program started.");
}
